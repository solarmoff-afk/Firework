use firework::ui;
// use firework::component;

ui! {
    fn test_screen() {
        
        for i in 0..5 {
            let mut b: Vec<u32> = spark!(Vec::new());
            (b).push(10);
        
            break;

            println!("123";)

        }
        println!("123")
    }
}

fn main() {
    test_screen();
}
