use firework::ui;

ui! {
    fn test_screen() { 
        let mut spark1 = spark!(100);
        let (x, x2) = (10, 10);

        // let mut spark1 = 5;

        // print(spark!(100));

        // let mut spark1 = 123;
        // let a = 1;
        // let mut spark2 = spark!(100);

        // rect!().width(spark1);
    }
}

fn main() {
    test_screen();
}
